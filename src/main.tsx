import React from "react";
import ReactDOM from "react-dom/client";
import WhaleExplorer from "../components/WhaleExplorer";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <WhaleExplorer />
  </React.StrictMode>
);
